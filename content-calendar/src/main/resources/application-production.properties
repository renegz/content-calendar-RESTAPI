cc.welcome-message=Esto es PRODUCCION
cc.about=Mensaje de about de produccion

#Ya que esto es produccion podemos colocar aqui la configuracion de la BD de PostgreSQL
#.datasource.url=jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
#spring.datasource.username=${PGUSER}
#spring.datasource.password=${PGPASSWORD}

#spring.sql.init.mode=always